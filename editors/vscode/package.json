{
  "name": "viztruct",
  "displayName": "Viztruct",
  "description": "Analyze and optimize Go struct memory layouts from inside VSCode.",
  "version": "0.0.1",
  "publisher": "buarki",
  "repository": {
    "type": "git",
    "url": "https://github.com/buarki/viztruct.git",
    "directory": "editors/vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:go"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "viztruct.analyzeStruct",
        "title": "Viztruct: Analyze struct layout"
      }
    ],
    "configuration": {
      "title": "Viztruct",
      "properties": {
        "viztruct.binaryPath": {
          "type": "string",
          "default": "",
          "description": "Override for the viztruct binary path. Leave empty to use the binary bundled with the extension. Set to an absolute path (or just `viztruct` to use PATH) to override."
        },
        "viztruct.timeoutSeconds": {
          "type": "number",
          "default": 30,
          "description": "Per-invocation timeout for the viztruct subprocess."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p .",
    "typecheck": "tsc --noEmit -p .",
    "build:binaries": "bash scripts/build-binaries.sh",
    "vscode:prepublish": "npm run compile && npm run build:binaries",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^5.3.0"
  }
}
